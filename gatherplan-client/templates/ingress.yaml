apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gatherplan-client
  labels:
    {{- include "gatherplan-client.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  rules:
    - host: test.gatherplan.site
      http:
        paths:
          - backend:
              service:
                name: gatherplan-client
                port:
                  number: 3000
            path: /
            pathType: ImplementationSpecific
